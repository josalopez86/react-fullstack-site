# back-end project
This project is built with nodeJS, express, mongoose and mongoDB also use docker to handle the database instance.

1. Install dependencies `npm install`
2. build database`docker-compose up -d`
3. To run the app `npm run dev`

